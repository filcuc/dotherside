name: CompileAndTest

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
    - uses: actions/checkout@v2
    - name: Pull image
      uses: docker://docker.pkg.github.com/filcuc/dotherside/dotherside:mr82
  
